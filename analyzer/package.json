{
  "name": "work-recorder-analyzer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local pipeline: screen-recorder session -> chunked Gemini analysis -> reusable workflow JSON",
  "scripts": {
    "analyze": "node --experimental-strip-types analyze.ts",
    "latest": "node --experimental-strip-types analyze.ts",
    "watch": "node --experimental-strip-types analyze.ts --watch",
    "skill-page": "node --experimental-strip-types to-skill-page.ts",
    "test": "node --experimental-strip-types --test to-skill-page.test.ts"
  },
  "engines": {
    "node": ">=22.6.0"
  }
}
